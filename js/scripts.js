jQuery(function($){

'use strict';


  // -------------------------------------------------------------
  // Flex Slider
  // -------------------------------------------------------------

    (function () {
    $(document).ready(function() {
                $('.carousel').carousel()
                $(".carousel-inner .item:first").addClass("active");
            });
    }());

	
});